# Dependencies
node_modules/

# Build and test output
dist/
coverage/
.vitest/
*.tsbuildinfo

# Worktrees and local agent state
.worktrees/
.pi-subagents/
.tmp/
.tmp-*/

# .NET/MSBuild intermediate output (Windows reference fixtures).
**/obj/

# Rust (M3 Desktop Companion, apps/companion) build artifacts. Cargo.lock is
# committed for the binary crate to keep Companion builds reproducible.
/target/
