# Qualigence Team Self-hosted — Linux single-node Docker Compose topology.
#
# The reverse proxy is the public HTTP entrypoint. Ticket 12 intentionally
# publishes one additional Server Runner gRPC port for external Runner mTLS;
# PostgreSQL and MinIO stay on the internal network with NO host-published ports.
# Every secret is mounted
# from a file under /run/secrets (never baked into an image layer or passed as a
# plaintext environment value). All third-party images are pinned by digest and
# `latest` is never used. Runtime application containers are non-root, run with a
# read-only root filesystem, drop all Linux capabilities, forbid privilege
# escalation and carry CPU/memory/PID and log-rotation limits.

name: qualigence-self-hosted

x-node-image: &node-image
  image: qualigence/self-hosted:${QUALIGENCE_IMAGE_TAG:-0.1.0}
  build:
    context: ../../..
    dockerfile: Dockerfile

x-hardening: &hardening
  read_only: true
  security_opt:
    - "no-new-privileges:true"
  cap_drop:
    - ALL
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

services:
  postgres:
    image: postgres:17-bookworm@sha256:4f736ae292687621d4dbe0d499ffd024a36bd2ee7d8ca6f2ccd4c800f047b394
    environment:
      POSTGRES_USER: qualigence_owner
      POSTGRES_DB: qualigence
      POSTGRES_PASSWORD_FILE: /run/secrets/pg_admin_password
    secrets:
      - pg_admin_password
    security_opt:
      - "no-new-privileges:true"
    networks:
      - internal
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qualigence_owner -d qualigence"]
      interval: 10s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 1G
          pids: 512
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  minio:
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z@sha256:1dce27c494a16bae114774f1cec295493f3613142713130c2d22dd5696be6ad3
    # Read the root credentials from mounted secret files so no secret value
    # ever appears in this service's environment.
    entrypoint:
      - /bin/sh
      - -c
      - |
        export MINIO_ROOT_USER="$$(cat /run/secrets/s3_access_key_id)"
        export MINIO_ROOT_PASSWORD="$$(cat /run/secrets/s3_secret_access_key)"
        exec minio server /data --console-address :9001
    secrets:
      - s3_access_key_id
      - s3_secret_access_key
    security_opt:
      - "no-new-privileges:true"
    networks:
      - internal
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD-SHELL", "mc ready local || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
          pids: 512
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # One-shot: provision the artifact bucket that Server/Worker readiness probes
  # and data-plane operations use. It talks to MinIO with the same configured S3
  # credentials rather than treating MinIO's unauthenticated health URL as proof
  # that the Qualigence bucket/policy is usable.
  minio-bucket:
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z@sha256:1dce27c494a16bae114774f1cec295493f3613142713130c2d22dd5696be6ad3
    entrypoint:
      - /bin/sh
      - -ec
    command:
      - |
        access_key="$$(cat /run/secrets/s3_access_key_id)"
        secret_key="$$(cat /run/secrets/s3_secret_access_key)"
        mc alias set qualigence http://minio:9000 "$${access_key}" "$${secret_key}"
        mc mb --ignore-existing qualigence/qualigence-artifacts
    depends_on:
      minio:
        condition: service_healthy
    secrets:
      - s3_access_key_id
      - s3_secret_access_key
    security_opt:
      - "no-new-privileges:true"
    networks:
      - internal
    tmpfs:
      - /tmp
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 128M
          pids: 128
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # One-shot: provision the schema, forced RLS and least-privilege roles. Run
  # explicitly (`docker compose run --rm migrate`) before starting the stack and
  # again before any upgrade. Never runs implicitly at Server startup.
  migrate:
    <<: [*node-image, *hardening]
    command: ["migrate"]
    profiles: ["setup"]
    depends_on:
      postgres:
        condition: service_healthy
    environment: &admin-env
      ADMIN_PG_HOST: postgres
      ADMIN_PG_PORT: "5432"
      ADMIN_PG_DATABASE: qualigence
      ADMIN_PG_USER: qualigence_owner
      ADMIN_PG_PASSWORD_FILE: /run/secrets/pg_admin_password
      ADMIN_SERVER_PG_USER: ${QUALIGENCE_SERVER_PG_ROLE:?set the Server PostgreSQL role}
      ADMIN_SERVER_PG_PASSWORD_FILE: /run/secrets/pg_server_password
      ADMIN_WORKER_PG_USER: qualigence_worker
      ADMIN_WORKER_PG_PASSWORD_FILE: /run/secrets/pg_worker_password
      ADMIN_S3_ENDPOINT: http://minio:9000
      ADMIN_S3_REGION: us-east-1
      ADMIN_S3_BUCKET: qualigence-artifacts
      ADMIN_S3_ACCESS_KEY_ID_FILE: /run/secrets/s3_access_key_id
      ADMIN_S3_SECRET_ACCESS_KEY_FILE: /run/secrets/s3_secret_access_key
      ADMIN_S3_FORCE_PATH_STYLE: "true"
      ADMIN_KMS_ROOT_KEY_FILE: /run/secrets/kms_root_key
      ADMIN_SERVER_BASE_URL: http://server:8080
      ADMIN_BACKUP_DIR: /var/lib/qualigence/backups
    secrets:
      - pg_admin_password
      - pg_server_password
      - pg_worker_password
      - s3_access_key_id
      - s3_secret_access_key
      - kms_root_key
    networks:
      - internal
    tmpfs:
      - /tmp
    volumes:
      - backups:/var/lib/qualigence/backups

  # Operator maintenance one-shots. Usage:
  #   docker compose run --rm doctor
  #   docker compose run --rm backup
  #   docker compose run --rm restore
  doctor:
    <<: [*node-image, *hardening]
    command: ["doctor", "--json"]
    profiles: ["ops"]
    environment: *admin-env
    secrets:
      - pg_admin_password
      - pg_server_password
      - pg_worker_password
      - s3_access_key_id
      - s3_secret_access_key
      - kms_root_key
    networks:
      - internal
    tmpfs:
      - /tmp
    volumes:
      - backups:/var/lib/qualigence/backups

  backup:
    <<: [*node-image, *hardening]
    command: ["backup"]
    profiles: ["ops"]
    depends_on:
      postgres:
        condition: service_healthy
    environment: *admin-env
    secrets:
      - pg_admin_password
      - pg_server_password
      - pg_worker_password
      - s3_access_key_id
      - s3_secret_access_key
      - kms_root_key
    networks:
      - internal
    tmpfs:
      - /tmp
    volumes:
      - backups:/var/lib/qualigence/backups

  restore:
    <<: [*node-image, *hardening]
    command: ["restore"]
    profiles: ["ops"]
    environment: *admin-env
    secrets:
      - pg_admin_password
      - pg_server_password
      - pg_worker_password
      - s3_access_key_id
      - s3_secret_access_key
      - kms_root_key
    networks:
      - internal
    tmpfs:
      - /tmp
    volumes:
      - backups:/var/lib/qualigence/backups

  server-volume-permissions:
    <<: [*node-image, *hardening]
    user: "0:0"
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        mkdir -p /var/lib/qualigence/artifacts /var/lib/qualigence/skill-signing
        chown 0:0 /var/lib/qualigence/artifacts /var/lib/qualigence/skill-signing
        chmod 0770 /var/lib/qualigence/artifacts /var/lib/qualigence/skill-signing
        chown -R 1000:1000 /var/lib/qualigence/artifacts /var/lib/qualigence/skill-signing
    network_mode: none
    cap_add:
      - CHOWN
    volumes:
      - artifactdata:/var/lib/qualigence/artifacts
      - skill_signing_data:/var/lib/qualigence/skill-signing
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 64M
          pids: 64

  server:
    <<: [*node-image, *hardening]
    command: ["server"]
    depends_on:
      server-volume-permissions:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      minio-bucket:
        condition: service_completed_successfully
    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: "8080"
      SERVER_PG_HOST: postgres
      SERVER_PG_PORT: "5432"
      SERVER_PG_DATABASE: qualigence
      SERVER_PG_USER: ${QUALIGENCE_SERVER_PG_ROLE:?set the Server PostgreSQL role}
      SERVER_PG_PASSWORD_FILE: /run/secrets/pg_server_password
      SERVER_OIDC_ISSUER: ${QUALIGENCE_OIDC_ISSUER:?set the OIDC issuer}
      SERVER_OIDC_AUDIENCE: ${QUALIGENCE_OIDC_AUDIENCE:?set the OIDC audience}
      SERVER_OIDC_ALGORITHMS: "RS256,ES256"
      SERVER_OIDC_JWKS_URI: ${QUALIGENCE_OIDC_JWKS_URI:?set the remote OIDC JWKS URI}
      SERVER_OIDC_JWKS_TIMEOUT_MS: "5000"
      SERVER_OIDC_JWKS_CACHE_TTL_MS: "600000"
      SERVER_OIDC_JWKS_ROTATION_COOLDOWN_MS: "30000"
      SERVER_OIDC_CLAIM_MAP_FILE: /run/secrets/oidc_claim_map
      SERVER_RUNNER_CA_CERT_FILE: /run/secrets/runner_ca_cert
      SERVER_RUNNER_CA_KEY_FILE: /run/secrets/runner_ca_key
      SERVER_RUNNER_GRPC_HOST: 0.0.0.0
      SERVER_RUNNER_GRPC_PORT: "50555"
      SERVER_RUNNER_GRPC_TLS_CERT_FILE: /run/secrets/runner_server_cert
      SERVER_RUNNER_GRPC_TLS_KEY_FILE: /run/secrets/runner_server_key
      SERVER_S3_ENDPOINT: http://minio:9000
      SERVER_S3_REGION: us-east-1
      SERVER_S3_BUCKET: qualigence-artifacts
      SERVER_S3_ACCESS_KEY_ID_FILE: /run/secrets/s3_access_key_id
      SERVER_S3_SECRET_ACCESS_KEY_FILE: /run/secrets/s3_secret_access_key
      SERVER_S3_FORCE_PATH_STYLE: "true"
      SERVER_KMS_ROOT_KEY_BASE64_FILE: /run/secrets/kms_root_key
      SERVER_ARTIFACT_DATA_DIR: /var/lib/qualigence/artifacts
      SERVER_SKILL_SIGNING_DATA_DIR: /var/lib/qualigence/skill-signing
    secrets:
      - pg_server_password
      - s3_access_key_id
      - s3_secret_access_key
      - kms_root_key
      - oidc_claim_map
      - runner_ca_cert
      - runner_ca_key
      - runner_server_cert
      - runner_server_key
    ports:
      - "${QUALIGENCE_RUNNER_GRPC_PORT:-50555}:50555"
    networks:
      - internal
    tmpfs:
      - /tmp
    volumes:
      - artifactdata:/var/lib/qualigence/artifacts
      - skill_signing_data:/var/lib/qualigence/skill-signing
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:8080/readyz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 12
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 768M
          pids: 512

  worker:
    <<: [*node-image, *hardening]
    command: ["worker"]
    depends_on:
      postgres:
        condition: service_healthy
      minio-bucket:
        condition: service_completed_successfully
    environment:
      WORKER_PG_HOST: postgres
      WORKER_PG_PORT: "5432"
      WORKER_PG_DATABASE: qualigence
      WORKER_PG_USER: qualigence_worker
      WORKER_PG_PASSWORD_FILE: /run/secrets/pg_worker_password
      WORKER_PG_SERVER_ROLE: ${QUALIGENCE_SERVER_PG_ROLE:?set the Server PostgreSQL role}
      WORKER_S3_ENDPOINT: http://minio:9000
      WORKER_S3_REGION: us-east-1
      WORKER_S3_BUCKET: qualigence-artifacts
      WORKER_S3_ACCESS_KEY_ID_FILE: /run/secrets/s3_access_key_id
      WORKER_S3_SECRET_ACCESS_KEY_FILE: /run/secrets/s3_secret_access_key
      WORKER_S3_FORCE_PATH_STYLE: "true"
      WORKER_HEALTH_HOST: 127.0.0.1
      WORKER_HEALTH_PORT: "8081"
      WORKER_MODEL_BASE_URL: ${QUALIGENCE_MODEL_BASE_URL:?set the model base URL}
      WORKER_MODEL_API_KEY_FILE: /run/secrets/worker_model_api_key
      WORKER_MODEL_NAME: ${QUALIGENCE_MODEL_NAME:?set the model name}
    secrets:
      - pg_worker_password
      - s3_access_key_id
      - s3_secret_access_key
      - worker_model_api_key
    networks:
      - internal
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:8081/readyz').then(r=>r.ok?process.exit(0):r.text().then(t=>{console.error(t);process.exit(1)})).catch((error)=>{console.error(error&&error.stack?error.stack:String(error));process.exit(1)})\""]
      interval: 10s
      timeout: 5s
      retries: 12
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 768M
          pids: 512

  console:
    image: qualigence/self-hosted-console:${QUALIGENCE_IMAGE_TAG:-0.1.0}
    build:
      context: ../../..
      dockerfile: deployments/self-hosted/docker/console.Dockerfile
    <<: *hardening
    # Caddy's upstream binary carries cap_net_bind_service; Docker refuses to
    # exec a file-capability binary when no-new-privileges is paired with a
    # fully empty capability set. Grant only that narrow capability so the
    # static Console can start while the service still has no host-published
    # ports and no added data-plane authority.
    cap_add:
      - NET_BIND_SERVICE
    command: ["caddy", "file-server", "--listen", ":8080", "--root", "/srv"]
    networks:
      - internal
    tmpfs:
      - /tmp
      - /config
      - /data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/ >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 12
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
          pids: 128

  proxy:
    image: caddy:2.8-alpine@sha256:af32e97399febea808609119bb21544d0265c58a02836576e32a2d082c262c17
    depends_on:
      server:
        condition: service_healthy
      console:
        condition: service_healthy
    ports:
      - "443:443"
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    environment:
      QUALIGENCE_SITE_ADDRESS: ${QUALIGENCE_SITE_ADDRESS:-:443}
    secrets:
      - tls_cert
      - tls_key
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddydata:/data
      - caddyconfig:/config
    networks:
      - internal
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          wget --no-check-certificate -qO- https://127.0.0.1/healthz >/dev/null
          && wget --no-check-certificate -qO- https://127.0.0.1/api/readyz >/dev/null
          && wget --no-check-certificate -qO- https://127.0.0.1/ >/dev/null
      interval: 10s
      timeout: 5s
      retries: 12
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M
          pids: 256

networks:
  internal:
    driver: bridge
    internal: false

volumes:
  pgdata:
  miniodata:
  backups:
  artifactdata:
  skill_signing_data:
  caddydata:
  caddyconfig:

# Every secret is a file mounted at /run/secrets/<name>. The files live under
# ./secrets and are git-ignored (see ./secrets/README.md for generation). No
# secret value is ever placed in an environment variable or image layer.
secrets:
  pg_admin_password:
    file: ./secrets/pg_admin_password
  pg_server_password:
    file: ./secrets/pg_server_password
  pg_worker_password:
    file: ./secrets/pg_worker_password
  s3_access_key_id:
    file: ./secrets/s3_access_key_id
  s3_secret_access_key:
    file: ./secrets/s3_secret_access_key
  kms_root_key:
    file: ./secrets/kms_root_key
  oidc_claim_map:
    file: ./secrets/oidc_claim_map.json
  runner_ca_cert:
    file: ./secrets/runner_ca_cert.pem
  runner_ca_key:
    file: ./secrets/runner_ca_key.pem
  runner_server_cert:
    file: ./secrets/runner_server_cert.pem
  runner_server_key:
    file: ./secrets/runner_server_key.pem
  worker_model_api_key:
    file: ./secrets/worker_model_api_key
  tls_cert:
    file: ./secrets/tls_cert.pem
  tls_key:
    file: ./secrets/tls_key.pem
